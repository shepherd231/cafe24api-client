import {
  Cafe24Boolean,
  Cafe24Date,
  Cafe24Datetime,
  Cafe24Enum,
} from "../types";
<%- event.alias.importFrom %>;


/**
 * @eventId <%= event.eventId %>
 *
<% if (event.description) { -%>
 * @description
<% for(const descline of event.description.split('\n')) { -%>
 * <%= descline %>
<% } -%>
<% } -%>
 */
export type <%= event.name %> = <%- event.alias.type %>;
