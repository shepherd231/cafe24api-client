import {
  Cafe24Boolean,
  Cafe24Date,
  Cafe24Datetime,
  Cafe24Enum,
} from "../types";

/**
 * @eventId <%= event.eventId %>
 *
<% if (event.description) { -%>
 * @description
<% for(const descline of event.description.split('\n')) { -%>
 * <%= descline %>
<% } -%>
<% } -%>
 */
export interface <%= event.name %> {
  <%- include('/recursive-prop-decl.ejs', { properties: event.properties }) -%>
}
